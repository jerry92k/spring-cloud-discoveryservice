# spring:
#   datasource:
#     driver-class-name: org.h2.Driver
#     url: jdbc:h2:tcp://h2/~/test
#     username: sa

token:
  expiration_time: 86400000
  secret: user_token_userservice

gateway:
  ip: 172.18.0.5

order_service:
  url: http://order-service/order-service/%s/orders
  exception:
    oders_is_empty: "there is no orders by a user"
  # ip port를 사용하는 대신 eureka에 등록되 msa 이름을 사용
  # url: http://127.0.0.1:8000/order-service/%s/orders
